# Prompt-template experiment results

Method: for each cell, the package under test is generated by the real
`maintain` CLI in a purpose-built fixture repository (see
`build_fixtures.py`), then answered by N independent fresh Haiku-model chat
conversations that see only the package. Replies are graded mechanically
(`grade.py`) with Maintain's own parsers, raw `git apply --recount --check`
in a pristine copy, real patch application, and real test runs.

Haiku is deliberately a weaker model than the chatbots the tool targets are
likely to be — a stress proxy. Sample sizes are small; treat rates as
directional except where a change moves a cell end to end (0/N → N/N).

## Round 1 (2026-07-28, n=24) — format compliance A/B

| Cell | Probe | Result | Decision |
| --- | --- | --- | --- |
| implement A/B | new-file patch, ± mode-line rule | raw apply A 0/4 → B 4/4 | **adopted** (implement.md, fix.md) + capture-time healing |
| review A/B | boundary bug, verdict-first vs verdict-last | catch 5/5 vs 4/5, one unparseable B reply | **rejected** — verdict-first retained; find_marker hardened for `## Verdict:` fusions |
| scope A/B | named-file request, ± write-permission rule | 3/3 exact both arms | no change (ceiling) |

## Round 2 (2026-07-28, n=77) — breadth, behaviour, and the untested steps

| Cell | Probe | n | Key rates |
| --- | --- | --- | --- |
| S1-cur | scope, named-file request (anchor) | 4 | exact allowed list 4/4 |
| S2-A / S2-B | scope, vague crash report ± write-permission rule | 6+6 | all 12 within tolerated set; A ≡ B → variant permanently shelved |
| I1-cur | implement, new-file creation (anchor, post-fix) | 5 | raw apply 5/5, behaviour 5/5 (round-1 fix holds) |
| I2-cur | implement, multi-hunk modify (rename + off-by-one + docstring) | 8 | raw apply 6/8, behaviour 6/8 — residual context-fidelity failures, absorbed by the correction loop |
| I3-cur | implement, delete file + rewrite import | 5 | 3/5 — both failures were RESCOPE_REQUIRED escalations: the package hid `utils.py` (only allowed files are packed) |
| **I3-v2** | same, package now includes Repository Structure | 5 | **5/5 everything** — confirms the packaging fix |
| R1-cur | review, boundary bug (anchor) | 4 | caught 4/4 |
| R2-cur | review, clean implementation (false-positive probe) | 7 | APPROVE 7/7 — zero spurious change requests |
| R3-A / R3-B | review, subtle rounding bug ± evidence-quoting rule | 7+7 | caught 7/7 both arms → variant not adopted (ceiling) |
| F1-cur | fix package after failing tests (first test of this step) | 7 | apply 7/7, tests pass after patch 7/7 |
| RS1-cur | rescope after scope contradiction (first test of this step) | 6 | markers 6/6, revised list includes the missing file 6/6 |

## Changes adopted from round 2

1. Implementation and correction packages now include the full repository
   structure (orientation only, write set unchanged). Motivated by I3-cur's
   2/5 spurious escalations ("No utils.py file exists in the current
   repository context"); confirmed by I3-v2 going 5/5.
2. `maintain` exits quietly on a closed stdout pipe (SIGPIPE during
   `maintain status | head`), found incidentally while regenerating
   fixtures.

## Known residuals (documented, not patched)

- Multi-hunk modifications of one file fail to apply first-try ~25% of the
  time with a weak model (I2: line-number drift and context inconsistency in
  large hand-authored hunks). The correction loop is the designed recovery;
  no template wording was adopted without A/B evidence that it helps. A
  future experiment: instruct one small hunk per edit site vs. current.
- All grading uses Claude models as the chatbot. Rates for Microsoft 365
  Copilot or other chatbots must be re-measured with real uploads; the
  harness supports that by hand-saving each chatbot reply under
  `replies/<cell>/<n>.md` and re-running `grade.py`.

## Round 3 (2026-07-28) — the harden workflow, measured on a real task

`maintain harden` was exercised on the `minisite` demo (a 3-task static site
generator): 4 implementation rounds, 1 rescope, 2 independent reviews.

| Measure | Before | After |
| --- | --- | --- |
| Line + branch coverage of targets | 98% | 100% (gate-enforced) |
| Tests | 57 | 78 |
| Mutation score (40 sampled mutants) | 60% | 85–87% |
| Target source files modified | — | none |

Two findings shaped the tool:

1. **A passing coverage gate is not a passing review.** Round 2 reached 100%
   coverage and the independent reviewer still returned CHANGES_REQUIRED,
   catching a tautological assertion (`assert str(parse_error.value) in err`
   — both sides derived from the same code path, so it cannot fail),
   unanchored `match="title"` patterns, and promised exact-document tests
   downgraded to substring checks. An independent mutation run agreed: the
   surviving mutants were precisely the template lines the reviewer flagged.
   The correction round moved the mutation score from 60% to ~86%.
2. **Hardening packages were missing their targets** (fixed). Packages pack
   Repomix context for the allowed files, which for a hardening task means
   the test files; the read-only target sources the new tests must assert
   against were absent, and the implementer correctly escalated
   RESCOPE_REQUIRED rather than invent expected strings. This is the same
   failure mode as round 2's I3 cell — packages under-supplying context —
   and is worth checking first whenever a new stage is added.

Residual on `minisite`: every remaining survivor is a `.strip()` removal, so
whitespace tolerance is genuinely untested in both modules — a gap neither
100% branch coverage nor a thorough reviewer surfaced, and the clearest
argument for treating mutation results as a gate rather than a footnote.
The measurement tool is `mutation_check.py` in this directory.

## Reproducing

```sh
python3 experiments/build_fixtures.py /tmp/eval-work
# answer each packages/<cell>.md with a fresh chatbot conversation,
# saving replies to /tmp/eval-work/replies/<cell>/<n>.md
python3 experiments/grade.py /tmp/eval-work
```
