# Task
Read `TASK.md`, the approved root cause, current complete files, actual diff, and every supplied finding. Correct every actionable finding without changing unauthorized paths or masking the root cause.

# Output contract
Follow the implementation output contract in `TASK.md` exactly. Return complete final contents for every added or modified file, not a patch or partial edit. Preserve `content.root_cause` with code-grounded `statement` and `evidence_paths`. Make no unsupported verification claims.
