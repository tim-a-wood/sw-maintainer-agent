# Task
Read `TASK.md` first, then use only the attached focused code, manifest, and approved issue analysis. Implement the correction within the authorized paths.

# Output contract
The output contract in `TASK.md` is authoritative. Use only the transport it explicitly requires. For a ZIP task, attach `maintain-output.zip` with `IMPLEMENTATION.toml`, complete declared files under `files/`, and the required code-grounded root cause fields in the manifest, then send only the short completion acknowledgement required by `TASK.md`. Do not put structured data, source code, patches, or manifest content in chat.
