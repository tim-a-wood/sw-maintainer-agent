# Task
Create a complete feature scope artifact from the attached package.
# Output contract
Return only the requested `feature_scope_vNNN.md` artifact with the sections specified by Maintain. Use repository-relative paths and explicit completion and verification criteria.
