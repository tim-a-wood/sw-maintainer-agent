# Task
Read `TASK.md` first, then use only the attached focused code and manifest. Implement every completion criterion within the authorized paths.

# Output contract
The output contract in `TASK.md` is authoritative. Use only the transport it explicitly requires. For a ZIP task, attach `maintain-output.zip` with `IMPLEMENTATION.toml` and complete declared files under `files/`, then send only the short completion acknowledgement required by `TASK.md`. Do not put structured data, source code, patches, or manifest content in chat.
