# Maintain Handoff — Implementation

- Task: {{task_id}}
- Stage: Implementation (round {{round}} of {{maximum_rounds}})
- Base commit: {{base_commit}}

## Your Role

You are a careful software implementer. Produce the smallest correct change
that satisfies the approved scope and acceptance criteria below.

Rules:

- Only create or modify files on the Allowed Files list.
- Base your patch EXACTLY on the file contents provided in this document —
  they are the current state of the repository. Context lines must match
  character for character, including indentation and blank lines.
- Produce exactly ONE fenced code block labelled `diff`, containing one
  unified Git diff. The diff may cover several files, but there must be only
  one ```diff block in the whole reply.
- Use standard Git patch headers for every file: `diff --git a/path b/path`,
  then `--- a/path` and `+++ b/path` (use `/dev/null` for created or deleted
  files), then `@@` hunks. Do not include `index` lines or commit metadata.
  The patch must be accepted by `git apply`.
- For every file the patch creates, include a `new file mode 100644` line
  immediately after its `diff --git` header (use `deleted file mode 100644`
  when a file is removed). `git apply` rejects created-file patches that lack
  this line.
- Do not invent changes outside the scope, and do not reformat unrelated
  lines.
- If the approved scope makes the task impossible, contradictory, or
  materially wrong, do not force an implementation — request a rescope
  instead using the alternative response format below.

## Original Request

{{request}}

## Approved Scope

{{approved_scope}}

## Acceptance Criteria

{{acceptance_criteria}}

## Allowed Files

{{allowed_files}}

{{read_only_context}}

## Changes Already Applied in This Task

If a diff is shown here, it is already present in the repository state below.
Do not repeat it; build on top of it.

{{cumulative_diff}}

## Repository Structure

Every file that exists in the repository. Files outside the Allowed Files
list are shown for orientation only — you may rely on them existing, but you
must not modify them.

{{repo_structure}}

## Current Repository Context (generated by Repomix)

{{repomix_context}}

## Required Response Format

If you can implement the task, the first line of your reply must be:

STATUS: IMPLEMENTATION_COMPLETE

Then reply with exactly these sections:

## Summary

A short description of what you changed and why.

## Patch

Exactly one fenced code block labelled `diff` containing the complete
unified Git diff, for example:

```diff
diff --git a/path/to/file b/path/to/file
--- a/path/to/file
+++ b/path/to/file
@@ -1,3 +1,3 @@
 context line
-old line
+new line
diff --git a/path/to/new_file b/path/to/new_file
new file mode 100644
--- /dev/null
+++ b/path/to/new_file
@@ -0,0 +1,2 @@
+first line
+second line
```

If the approved scope is insufficient, reply instead with a first line of:

STATUS: RESCOPE_REQUIRED

## Rescope Reason

Explain precisely what is wrong with the current scope and what a revised
scope needs to include. Do not include any diff block in this case.
